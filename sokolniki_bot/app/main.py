"""
Bot production instance runs on Timeweb VPS (37.252.21.88).
This Replit workflow is intentionally disabled to avoid polling conflicts.
"""
import logging
logging.basicConfig(level=logging.INFO)
logging.getLogger(__name__).info(
    "Bot is deployed on the VPS server. This Replit instance is disabled."
)
